---
id: task.cli-check-archive-edit-search-overview
type: task
state: closed
title: CLI: check / archive / edit / search / overview
by: Maksim Yaromin
via: claude-code
tags: cli
link: sha 14ea75d
blocked-by: task.core-grammar-parser-renderer
blocked-by: task.core-record-model-invariants
created: 2026-08-29
updated: 2026-08-29
closed: 2026-08-29
---

Check names file, line, reason; archive moves records with history; search and overview ship in v1. Adapter duty: a non-UTF-8 file must surface through the CLI as the not-utf8 finding — visible to the calling LLM, never a crash or a silent skip; the Core cannot detect it (Storage feeds it strings), so the fs adapter owns it.
- 2026-08-29 claude-code: Field evidence from the self-host migration: imported titles and bodies could not be touched after add — no edit surface — and four dangling-mention debt rows now sit in closed imported bodies with no repair path; edit/archive is what unlocks it.
- 2026-08-29 Maksim Yaromin: Field finding while testing 2026-08-29: 'anb archive' answers with raw clap usage (exit 2), not the structured error contract — no error[<stable-code>], no try: lines. An agent typing an unknown or not-yet-built verb gets no recovery path. When this task ships the archive verb, also decide whether unknown-subcommand errors join the structured catalog (e.g. error[unknown-command] + try: listing the nearest verbs) or stay clap's.
- 2026-08-29 claude-code: Plan settled after spec+code dig: (1) check verb renders Notebook::check as a bounded findings table {file,line,severity,code,message}, exit 1 on error findings; (2) archive = byte-identical move of a settled record (task closed, decision superseded/retired, note retired, question routed/dropped), replay already:true, live record refused via invalid-transition naming the settling commands; (3) edit mutates title/body/tag/untag/from/priority/review-by on live records of any state — states stay commands, body replace carries the dangling-mention nudge; (4) search = case-insensitive substring over id/title/tags/body, live+archive, list-shaped rows; (5) overview = unbounded one-pager grouped by type plus archive counts; (6) fs adapter gains StorageError::NotUtf8, the Core maps it to a not-utf8 finding (new FindingCode) so a binary file surfaces in check/list/debt instead of aborting the command; (7) unknown subcommands join the catalog as error[unknown-command] with clap's suggestion as a try: line; (8) found drift fixed beside the change: list/overview/search show invalid records as state=invalid per format spec, instead of list silently excluding them.
- 2026-08-29 claude-code: Implementation complete, gate green (fmt, clippy -D warnings, 227 core + 77 CLI + 11 fs tests, red-proofs run per module). Shipped: check (findings table {file,line,severity,code,message}, exit 1 on error findings), archive (byte-identical move, replay already, refusal names settling commands), edit (--title/--body/--tag/--untag/--from/--priority/--review-by, splice-only-what-differs, replay already, dangling-mention nudge on new bodies), search (case-insensitive substring over id/title/tags/body, live+archive, list-shaped rows), overview (unbounded one-pager by type + archive counts), StorageError::NotUtf8 + FindingCode::NotUtf8 (binary file = visible invalid record in check/list, error[not-utf8] on view, id stays claimed), error[unknown-command] with clap suggestions as try: lines. Found-and-fixed beside the change: list silently excluded invalid records — now rows with state=invalid per the format spec. Smoke-tested all verbs against a copy of the real notebook. Next: Opus 5 review.
- 2026-08-29 claude-code: Opus 5 review returned 1 must-fix, 13 should-fix, 11 nits — all accepted and fixed: (must) std::env::args panicked on a non-UTF-8 argument, now args_os; overview/status counts now share one Core-computed tally (Overview.live, also in JSON); absent title is Option, rendered - / omitted; five new JSON shapes now tested; search hint single-quotes the query (double quotes left $ live); unknown-command tries are runnable (anb <verb> --help); archive refuses to overwrite a divergent archived copy (duplicate-id) and the write-then-remove ordering is tested with a failing-remove adapter; canonical body shape has one home (create renders through edited_body); edit's splice extracted to a named free fn; byte-exactness test now uses a BOM/CRLF fixture and is red-proven; settled-type test asserts the observable move; check_config not-utf8, invalid rows in search/overview, priority/review-by happy path, check bounds all covered; Recovery moved to reply.rs breaking the cli->text cycle; counts_phrase re-exported; restatement comments deleted; falsified doc claims reworded. Reviewer's handoff finding filed as question.should-state-vs-residence-disagreement-b (state vs residence split is not yet a Check finding). Gate green: 233 core + 85 CLI + 11 fs tests.
