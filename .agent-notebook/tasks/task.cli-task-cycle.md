---
id: task.cli-task-cycle
type: task
state: closed
title: CLI: task cycle
by: Maksim Yaromin
via: claude-code
tags: cli
link: report .tmp/data/l1/report.md
blocked-by: task.core-record-model-invariants
blocked-by: task.core-dependency-graph
created: 2026-08-29
updated: 2026-08-29
closed: 2026-08-29
---

add/start/submit/close/reopen/hold/unhold/block/unblock/comment/ready/list/view/status over fs Storage; output contract (leading ok: line, transitions + computed consequences, --json everywhere); invalid transition answers with the list of valid ones. Owner's call 2026-08-28 (c4 session): the status command surface lands here — anb status with --budget <n> (0 = no ceiling, overriding the config key), --json, and --hook for the gated session-start payload; c4 hands over the assembled, budgeted Status model.

Progress 2026-08-28 (claude, session 1) — plan settled after reading interaction/record-model/concept specs and the Core: (1) Core gains comment (log append, idempotent via last-line equality, any non-archived Task), list (live valid records, no filters in v1), view (fields in envelope order + body + mentions/mentioned-by via the scan). (2) CLI crate: lib+thin-main shape, fs Storage adapter (atomic temp+rename write), root = nearest ancestor with .agent-notebook else git root else cwd; today from jiff (local); by from git config user.name (--by overrides); via = flag only, no env detection. (3) Commands: add start submit close return reopen hold unhold block unblock comment ready list view status (return included: the transition table names it and invalid-transition payloads suggest it). (4) Output contract per interaction spec §2–3; replay renders "(already)"; errors to stderr as error[code] + computed try: lines, exit 1; --json compact everywhere (kebab keys, view fields as ordered pairs). (5) status --budget (flag > config), --json, --hook (Claude Code payload with data-framing line; fail-soft exit 0 empty stdout). (6) Deps verified on crates.io today: jiff 0.2.35, serde_json 1.0.151, tempfile 3.27.0 (dev), insta 1.48 (dev). Deliberately out: lockfile concurrency (flagged for owner), check/archive/search (l3), decide/note/ask/answer (l2), retire surface (absent from command vocabulary).

Progress 2026-08-28 (claude, session 1, cont.): implemented. Core: comment/list/view on Notebook, RecordFile::fields(), encode.rs (shared table quoting, moved out of status.rs); fixed a leftover ADR citation in anb-core/Cargo.toml. CLI crate reshaped lib+thin-main: cli.rs (clap surface), app.rs (execute -> Reply), render.rs (plain contract + Recovery payloads with computed try: lines), json.rs (compact JSON, preserve_order for key order; hook payload with data framing), fs_storage.rs (fs adapter, atomic temp+rename, resolve_root), main.rs (wiring: jiff local date, git user.name, hook fail-soft exit 0). Tests: 16 new Core (task_log/listing/record_view), 29 CLI golden (insta inline, expectations hand-derived from the interaction contract incl. the spec's ready worked example), 10 fs+root. Can-fail proven: 55 expectation flips, all red, restored. Gate green: fmt, clippy pedantic -D warnings, 270 tests. Binary smoke-tested end to end in a temp repo (add/block/start/comment/ready/status/close-with-unblocked/hook/view/error exit 1). Next: Opus 5 review.

Opus 5 review 2026-08-28: 22 findings (4 must-fix, 11 should-fix, 7 judgment calls) — fixed test-first: try: lines now computed for invalid-argument (verb+id Subject context), would-cycle (unblock suggestions from the walked chain), archived/wrong-type/invalid-record (findings detail + view suggestion); ready row + age moved into Core encode (one home, CLI dropped jiff date math; grammar::day_number now pub); false preserve_order comment corrected; glossary-forbidden Dashboard renamed to Reply::Status everywhere incl. --help; hook fail-soft moved into execute as Reply::Silence (now tested); Held/Edged string tags split into Held/Unheld/Blocked/Unblocked variants; bound/ROW_BOUND single home in reply.rs; Created.superseded now rendered; git identity lazy (FnOnce, forked only for add/comment); UnknownId/DanglingRef arms split; resolve_root bounded at the repository (.git) boundary; app.rs->reply.rs, render.rs->text.rs; doc fixes (execute # Errors, comment # Errors, ListedRecord, priority help). New tests: budget flag outranks config key, hook fail-soft + non-hook contrast, ready/list --json bounding, json priority omission, block/unblock replies + replays, comment replay mark, invalid-record/archived/wrong-type payloads, bounded root walk, 2 encode row tests. Positions held, reported honestly: (a) CLI goldens keep pinning full payload text incl. Core message prose — the concept spec's testing decision makes stdout the behavior under golden e2e; (b) insta inline snapshots kept for multi-line goldens, expectations hand-derived, 19 more can-fail flips all red (74 total); (c) vocabulary-parse table test kept as the one surface-contract guard. Known follow-ups for the owner: 'dashboard' wording persists in pre-existing committed Core prose/test names (glossary Avoid-list drift, predates this diff); no lockfile concurrency in l1 (atomic temp+rename only); no retire CLI surface (absent from command vocabulary). Gate green after all fixes: fmt, clippy pedantic -D warnings, 284 tests; binary re-smoke-tested (recovery payloads, hook, list --json). Working tree unstaged, uncommitted — REVIEW PAUSE.
