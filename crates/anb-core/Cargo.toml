[package]
name = "anb-core"
description = "agent-notebook Core: grammar, record model, invariants, and queries behind the Storage seam"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde_json.workspace = true

[dev-dependencies]
proptest.workspace = true
yaml-rust2 = { version = "0.13", default-features = false }

[lints]
workspace = true
